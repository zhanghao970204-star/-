# 合作活动（旧版规则页）

## 入口与现状

路由 `/activityCooperate` 保留该页面，但当前活动中心和旧 Bonus 列表均无入口。

## 页面行为

页面展示多语言合作说明，并提供外部 Telegram 链接按钮。默认链接为 `https://t.me/OTgamel`；代码保留了通过 `GetCsLink` 动态取客服链接的实现，但当前调用已注释。

## 维护提示

这是外链引导页，没有参与资格、奖励领取或结算逻辑。重新启用前应确认外链地址和客服接口是否仍有效。

## 相关实现

- 页面：`src/views/details/bonus/activityCooperate.vue`
- 可选接口：`/getCsLink`（当前未调用）
