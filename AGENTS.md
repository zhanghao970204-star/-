# 前端协作与省 Token 规范

## 进入项目时

- 每次开始处理本仓库前，先完整阅读本文件；它是本项目的最高优先级协作约定（低于系统、开发者和用户当次指令）。
- 默认仅处理用户明确指定的 Cocos 或 Vue 范围；信息不足时优先从相邻文件、调用点和现有约定中推断，不做无关扫描。
- 不重复输出已确认的背景、文件内容或长篇执行计划；先给结论，再给必要依据。

## 读取策略（省 Token）

- 先用 `rg --files`、`rg` 精确定位；只读取相关文件和必要上下文，优先使用带行号的局部片段。
- 首先查看：目标文件、其直接引用/调用方、相关类型或配置；确认无关后立即停止继续展开。
- 不遍历 `node_modules`、构建产物、缓存、覆盖率、截图或大体积资源，除非用户明确要求。
- 不为简单改动搜索整个仓库；不读取重复的 lockfile、生成文件或压缩资源。
- 提问、说明和提交总结保持简短；不复述命令输出。

## 修改策略

- 遵循现有项目写法、目录结构和命名；只做完成需求所必需的最小改动。
- 修改前先检查工作区状态与目标文件的未提交改动；不得覆盖或回退用户的既有改动。
- 优先使用已有依赖、组件、工具函数与资源；不要无故新增依赖、重构或格式化无关文件。
- Vue：保持 Vue 3 + TypeScript + Vite 的既有模式，变更时关注 props、emits、响应式、路由及 Pinia 调用链。
- Cocos：优先复用现有组件、节点路径、生命周期和资源加载方式；避免全局查找节点、重复加载资源和无必要的每帧逻辑。

## 验证与构建

- **默认不执行任何打包、构建、部署或预览命令。** 包括 `npm run build`、`npm run build:prod`、`vite build`、`vue-tsc`、所有 `deploy*` 脚本及等价命令。
- 每次代码修改完成后，不自动进行打包/构建流程；仅报告已做的静态检查或用户明确要求的验证。
- 只有用户明确要求运行某个验证命令时才执行，且只执行该命令；若用户要求构建/部署，先按其指定环境执行。
- 可在不触发构建的前提下进行轻量核对：检查 diff、搜索引用、审阅类型与语法、或运行用户点名的局部测试。

## 交付

- 最终答复只说明：改了什么、涉及哪些文件、是否执行验证；无需重复过程。
- 如未运行验证，应直说“未执行构建/打包（遵循项目约定）”。
